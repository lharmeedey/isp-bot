# SpeedNet ISP Telegram Bot

## Setup
1. Copy `.env.example` to `.env` and fill in your values
2. `npm install`
3. `node src/bot.js`

## Commands
Customer: /start /register /balance /buy /history /support
Admin:    /sales /users /online /stock /revenue
